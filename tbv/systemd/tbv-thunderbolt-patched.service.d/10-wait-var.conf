[Unit]
# /var is a separate btrfs mount that comes up ~7.2s, AFTER this unit was being
# evaluated (~6.3s). Wait for it so ConditionPathExists=/var/lib/tbv/... is met.
RequiresMountsFor=/var/lib/tbv
# load the patched core before the thunderbolt manager starts
Before=bolt.service
